services:
  - type: web
    name: leadspark-backend
    runtime: python
    buildCommand: pip install -r packages/backend/requirements.txt
    startCommand: uvicorn packages.backend.main:app --host 0.0.0.0 --port 10000
    plan: free
    autoDeploy: true
    rootDir: packages/backend
