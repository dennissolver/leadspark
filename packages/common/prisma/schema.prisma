// packages/common/prisma/schema.prisma

// This file defines your database schema and is used by the Prisma CLI.
// To learn more, visit https://pris.ly/d/prisma-schema

// The 'datasource' block configures your database connection.
// It specifies the database provider (e.g., PostgreSQL) and the connection URL,
// which is pulled from a environment variable for security.
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// The 'generator' block specifies what clients Prisma should generate.
// 'prisma-client-js' is the standard client for JavaScript and TypeScript.
generator client {
  provider = "prisma-client-js"
}

// Below are your data models, which represent the tables in your database.
// This is a simple starting point with a User model. You can add more models
// and relations as your application grows.

model User {
  id        String   @id @default(uuid())
  email     String   @unique
  password  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Optional fields
  firstName String?
  lastName  String?
}

// You can add other models, like a Tenant for multi-tenant applications.
// model Tenant {
//   id        String   @id @default(uuid())
//   name      String
//   plan      String
//   users     User[]   // A one-to-many relationship with the User model
//   createdAt DateTime @default(now())
//   updatedAt DateTime @updatedAt
// }
